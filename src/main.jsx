import React from 'react'
import ReactDOM from 'react-dom/client'
import WebcamApp from './WebcamApp.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WebcamApp />
  </React.StrictMode>,
)